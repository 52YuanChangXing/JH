version: '3.8'
services:
  db:
    image: postgres:15-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: jianhao_studio
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data:
